{
  "name": "ftnn-status",
  "version": "1.0.0",
  "private": true,
  "description": "FTNN status dashboard — Netlify functions (Clockify budget + podcast engagement).",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
